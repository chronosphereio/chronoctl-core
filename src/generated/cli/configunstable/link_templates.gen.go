// Code generated by chronogen; DO NOT EDIT
package configunstable

import (
	"context"
	"fmt"

	"github.com/chronosphereio/chronoctl-core/src/cmd/cli"
	"github.com/chronosphereio/chronoctl-core/src/cmd/pkg/client"
	"github.com/chronosphereio/chronoctl-core/src/cmd/pkg/clienterror"
	"github.com/chronosphereio/chronoctl-core/src/cmd/pkg/dry"
	"github.com/chronosphereio/chronoctl-core/src/cmd/pkg/file"
	"github.com/chronosphereio/chronoctl-core/src/cmd/pkg/groups"
	"github.com/chronosphereio/chronoctl-core/src/cmd/pkg/output"
	"github.com/chronosphereio/chronoctl-core/src/cmd/pkg/pagination"
	"github.com/chronosphereio/chronoctl-core/src/cmd/pkg/ptr"
	config_unstable "github.com/chronosphereio/chronoctl-core/src/generated/swagger/configunstable/client/operations"
	"github.com/chronosphereio/chronoctl-core/src/generated/swagger/configunstable/models"
	"github.com/chronosphereio/chronoctl-core/src/types"
	"github.com/spf13/cobra"
	flag "github.com/spf13/pflag"
)

func init() { types.MustRegisterObject(LinkTemplateTypeMeta, &LinkTemplate{}) }

var _ types.Object = &LinkTemplate{}

var LinkTemplateTypeMeta = types.TypeMeta{
	APIVersion: "unstable/config",
	Kind:       "LinkTemplate",
}

type LinkTemplate struct {
	types.TypeMeta `json:",inline"`
	Spec           *models.ConfigunstableLinkTemplate `json:"spec"`
}

func NewLinkTemplate(spec *models.ConfigunstableLinkTemplate) *LinkTemplate {
	return &LinkTemplate{
		TypeMeta: LinkTemplateTypeMeta,
		Spec:     spec,
	}
}

func (e *LinkTemplate) Description() string {
	return types.TypeDescription(e, "name", e.Spec.Name, "slug", e.Spec.Slug)
}

func (e *LinkTemplate) Identifier() string {
	return e.Spec.Slug
}

func CreateLinkTemplate(
	ctx context.Context,
	client config_unstable.ClientService,
	entity *LinkTemplate,
	dryRun bool,
) (*LinkTemplate, error) {
	res, err := client.CreateLinkTemplate(&config_unstable.CreateLinkTemplateParams{
		Context: ctx,
		Body: &models.ConfigunstableCreateLinkTemplateRequest{
			DryRun:       dryRun,
			LinkTemplate: entity.Spec,
		},
	})
	if err != nil {
		return nil, clienterror.Wrap(err)
	}
	return NewLinkTemplate(res.Payload.LinkTemplate), nil
}

func newLinkTemplateCreateCmd() *cobra.Command {
	var (
		permissiveParsing bool
		dryRunFlags       = dry.NewFlags()
		clientFlags       = client.NewClientFlags()
		outputFlags       = output.NewFlags(output.WithoutOutputDirectory(), output.WithoutCreateFilePerObject())
		fileFlags         = file.NewFlags(true /* required */)
	)

	var (
		use   string
		short string
	)
	use = "create -f <file>"
	short = "Creates a single LinkTemplate."

	cmd := &cobra.Command{
		Use:     use,
		GroupID: groups.Commands.ID,
		Short:   short,
		RunE: func(cmd *cobra.Command, args []string) error {
			ctx, cancel := context.WithTimeout(cmd.Context(), clientFlags.Timeout())
			defer cancel()
			if err := outputFlags.Validate(); err != nil {
				return err
			}
			defer outputFlags.Close(cmd.OutOrStdout())
			stderr := output.NewStderrPrinter(cmd)

			client, err := clientFlags.ConfigUnstableClient()
			if err != nil {
				return err
			}

			var linkTemplate *LinkTemplate
			file, err := fileFlags.File()
			if err != nil {
				return err
			}
			defer file.Close() //nolint:errcheck
			linkTemplate, err = types.MustDecodeSingleObject[*LinkTemplate](file, permissiveParsing)
			if err != nil {
				return err
			}

			if dryRunFlags.DryRun {
				stderr.Println("--dry-run is set")
			}
			fullLinkTemplate, err := CreateLinkTemplate(ctx, client, linkTemplate, dryRunFlags.DryRun)
			if err != nil {
				return err
			}

			if dryRunFlags.DryRun {
				stderr.Println("LinkTemplate is valid and can be created")
				return nil
			}
			stderr.Printf("LinkTemplate with slug %q created successfully\n", fullLinkTemplate.Spec.Slug)

			if err := outputFlags.WriteObject(fullLinkTemplate, cmd.OutOrStdout()); err != nil {
				return err
			}
			return nil
		},
	}
	dryRunFlags.AddFlags(cmd)
	clientFlags.AddFlags(cmd)
	outputFlags.AddFlags(cmd)
	fileFlags.AddFlags(cmd)
	cmd.Flags().BoolVar(&permissiveParsing, "no-strict", false, "If set, manifests with unknown fields are not rejected. Defaults to false.")

	return cmd
}

func GetLinkTemplate(
	ctx context.Context,
	client config_unstable.ClientService,
	slug string,
) (*LinkTemplate, error) {
	res, err := client.ReadLinkTemplate(&config_unstable.ReadLinkTemplateParams{
		Context: ctx,
		Slug:    slug,
	})
	if err != nil {
		return nil, clienterror.Wrap(err)
	}
	return NewLinkTemplate(res.GetPayload().LinkTemplate), nil
}

func newLinkTemplateReadCmd() *cobra.Command {
	clientFlags := client.NewClientFlags()
	outputFlags := output.NewFlags(output.WithoutOutputDirectory(), output.WithoutCreateFilePerObject())
	var (
		short string
		use   string
		args  cobra.PositionalArgs
	)
	short = "Reads a single LinkTemplate by slug"
	use = "read <slug>"
	args = cobra.ExactArgs(1)

	cmd := &cobra.Command{
		Use:     use,
		GroupID: groups.Commands.ID,
		Short:   short,
		Args:    args,
		RunE: func(cmd *cobra.Command, args []string) error {
			ctx, cancel := context.WithTimeout(cmd.Context(), clientFlags.Timeout())
			defer cancel()
			if err := outputFlags.Validate(); err != nil {
				return err
			}
			defer outputFlags.Close(cmd.OutOrStdout())

			client, err := clientFlags.ConfigUnstableClient()
			if err != nil {
				return err
			}
			entity, err := GetLinkTemplate(ctx, client, args[0])
			if err != nil {
				return err
			}
			if err := outputFlags.WriteObject(entity, cmd.OutOrStdout()); err != nil {
				return err
			}
			return nil
		},
	}

	clientFlags.AddFlags(cmd)
	outputFlags.AddFlags(cmd)

	return cmd
}

func UpdateLinkTemplate(
	ctx context.Context,
	client config_unstable.ClientService,
	entity *LinkTemplate,
	opts UpdateOptions,
) (*LinkTemplate, error) {
	res, err := client.UpdateLinkTemplate(&config_unstable.UpdateLinkTemplateParams{
		Context: ctx,
		Slug:    entity.Spec.Slug,
		Body: &models.ConfigUnstableUpdateLinkTemplateBody{
			CreateIfMissing: opts.CreateIfMissing,
			DryRun:          opts.DryRun,
			LinkTemplate:    entity.Spec,
		},
	})
	if err != nil {
		return nil, clienterror.Wrap(err)
	}

	return NewLinkTemplate(res.Payload.LinkTemplate), nil
}

func newLinkTemplateUpdateCmd() *cobra.Command {
	var (
		permissiveParsing bool
		createIfMissing   bool
		dryRunFlags       = dry.NewFlags()
		clientFlags       = client.NewClientFlags()
		outputFlags       = output.NewFlags(output.WithoutOutputDirectory(), output.WithoutCreateFilePerObject())
		fileFlags         = file.NewFlags(true /* required */)
	)

	cmd := &cobra.Command{
		Use:     "update -f <filename>",
		GroupID: groups.Commands.ID,
		Short:   "Updates an existing LinkTemplate.",
		RunE: func(cmd *cobra.Command, args []string) error {
			ctx, cancel := context.WithTimeout(cmd.Context(), clientFlags.Timeout())
			defer cancel()
			if err := outputFlags.Validate(); err != nil {
				return err
			}
			defer outputFlags.Close(cmd.OutOrStdout())
			stderr := output.NewStderrPrinter(cmd)

			client, err := clientFlags.ConfigUnstableClient()
			if err != nil {
				return err
			}

			file, err := fileFlags.File()
			if err != nil {
				return err
			}
			defer file.Close() //nolint:errcheck

			linkTemplate, err := types.MustDecodeSingleObject[*LinkTemplate](file, permissiveParsing)
			if err != nil {
				return err
			}

			updateOpts := UpdateOptions{
				DryRun:          dryRunFlags.DryRun,
				CreateIfMissing: createIfMissing,
			}

			if dryRunFlags.DryRun {
				stderr.Println("--dry-run is set, update not persisted")
			}

			fullLinkTemplate, err := UpdateLinkTemplate(ctx, client, linkTemplate, updateOpts)
			if err != nil {
				return err
			}

			if dryRunFlags.DryRun {
				stderr.Println("LinkTemplate is valid and can be updated")
				return nil
			}
			stderr.Printf("LinkTemplate with slug %q applied successfully\n", fullLinkTemplate.Spec.Slug)

			if err := outputFlags.WriteObject(fullLinkTemplate, cmd.OutOrStdout()); err != nil {
				return err
			}
			return nil
		},
	}
	dryRunFlags.AddFlags(cmd)
	clientFlags.AddFlags(cmd)
	outputFlags.AddFlags(cmd)
	fileFlags.AddFlags(cmd)
	cmd.Flags().BoolVar(&permissiveParsing, "no-strict", false, "If set, manifests with unknown fields are allowed. Defaults to false.")
	cmd.Flags().BoolVar(&createIfMissing, "create-if-missing", false, "If set, creates the LinkTemplate if it does not already exist. Defaults to false.")

	return cmd
}

func DeleteLinkTemplate(
	ctx context.Context,
	client config_unstable.ClientService,
	slug string,
) error {
	_, err := client.DeleteLinkTemplate(&config_unstable.DeleteLinkTemplateParams{
		Context: ctx,
		Slug:    slug,
	})
	if err != nil {
		return clienterror.Wrap(err)
	}
	return nil
}

func newLinkTemplateDeleteCmd() *cobra.Command {
	clientFlags := client.NewClientFlags()
	outputFlags := output.NewFlags(output.WithoutOutputDirectory(), output.WithoutCreateFilePerObject())

	cmd := &cobra.Command{
		Use:     "delete <slug>",
		GroupID: groups.Commands.ID,
		Short:   "Deletes a single LinkTemplate by slug",
		Args:    cobra.ExactArgs(1),
		RunE: func(cmd *cobra.Command, args []string) error {
			ctx, cancel := context.WithTimeout(cmd.Context(), clientFlags.Timeout())
			defer cancel()
			if err := outputFlags.Validate(); err != nil {
				return err
			}
			defer outputFlags.Close(cmd.OutOrStdout())

			client, err := clientFlags.ConfigUnstableClient()
			if err != nil {
				return err
			}

			res, err := client.DeleteLinkTemplate(&config_unstable.DeleteLinkTemplateParams{
				Context: ctx,
				Slug:    args[0],
			})
			if err != nil {
				return clienterror.Wrap(err)
			}
			_ = res
			fmt.Fprintf(cmd.OutOrStdout(), "deleted LinkTemplate with slug %q\n", args[0])

			return nil
		},
	}
	clientFlags.AddFlags(cmd)
	outputFlags.AddFlags(cmd)
	return cmd
}

type LinkTemplateListOpts struct {
	// Limit represents that maximum number of items we wish to return.
	Limit int
	// PageToken is the pagination token we want to start our request at.
	PageToken string
	// PageMaxSize is the maximum page size to use when making List calls.
	PageMaxSize  int
	Names        []string
	Slugs        []string
	UIComponents []string
}

func (r *LinkTemplateListOpts) registerFlags(flags *flag.FlagSet) {
	var emptyNames []string
	flags.StringSliceVar(&r.Names, "names", emptyNames, "Filters results by name, where any LinkTemplate with a matching name in the given list (and matches all other filters) is returned.")
	var emptySlugs []string
	flags.StringSliceVar(&r.Slugs, "slugs", emptySlugs, "Filters results by slug, where any LinkTemplate with a matching slug in the given list (and matches all other filters) is returned.")
	var emptyUIComponents []string
	flags.StringSliceVar(&r.UIComponents, "ui-components", emptyUIComponents, "Filters results by ui_component, where any LinkTemplate with a matching ui_component in the given list (and matches all other filters) is returned.")
	flags.IntVar(&r.Limit, "limit", 0, "maximum number of items to return")
	flags.IntVar(&r.PageMaxSize, "page-max-size", 0, "maximum page size")
	flags.StringVar(&r.PageToken, "page-token", "", "begins listing items at the start of the pagination token")
}

func ListLinkTemplates(
	ctx context.Context,
	client config_unstable.ClientService,
	streamer output.Streamer[*LinkTemplate],
	opts LinkTemplateListOpts,
) (pagination.Token, error) {
	var (
		gotItems    = 0
		nextToken   = opts.PageToken
		pageMaxSize = opts.PageMaxSize
	)

	// Use the limit if it's set, and smaller than a set page size.
	if opts.Limit > 0 && (opts.Limit < pageMaxSize || pageMaxSize == 0) {
		pageMaxSize = opts.Limit
	}

	for {
		res, err := client.ListLinkTemplates(&config_unstable.ListLinkTemplatesParams{
			Context:      ctx,
			PageToken:    &nextToken,
			PageMaxSize:  ptr.Int64(int64(pageMaxSize)),
			Names:        opts.Names,
			Slugs:        opts.Slugs,
			UIComponents: opts.UIComponents,
		})
		if err != nil {
			return pagination.Token(""), clienterror.Wrap(err)
		}

		for _, v := range res.Payload.LinkTemplates {
			if err := streamer(NewLinkTemplate(v)); err != nil {
				return pagination.Token(""), err
			}
			gotItems++
		}

		nextToken = res.Payload.Page.NextToken
		if nextToken == "" {
			return pagination.Token(""), nil
		}

		if opts.Limit > 0 && gotItems >= opts.Limit {
			return pagination.Token(nextToken), nil
		}

		pageMaxSize = pagination.CalculatePageSize(pagination.Calculation{
			GotItems:    gotItems,
			MaxItems:    opts.Limit,
			MaxPageSize: len(res.Payload.LinkTemplates),
		})
	}
}

func newLinkTemplateListCmd() *cobra.Command {
	var listOptions LinkTemplateListOpts
	clientFlags := client.NewClientFlags()
	outputFlags := output.NewFlags()

	cmd := &cobra.Command{
		Use:     "list",
		Short:   "Lists all LinkTemplates and applies optional filters",
		GroupID: groups.Commands.ID,
		RunE: func(cmd *cobra.Command, args []string) error {
			ctx, cancel := context.WithTimeout(cmd.Context(), clientFlags.Timeout())
			defer cancel()
			if err := outputFlags.Validate(); err != nil {
				return err
			}

			writer, err := outputFlags.NewWriterManager(cmd.OutOrStdout())
			if err != nil {
				return err
			}
			defer writer.Close()

			client, err := clientFlags.ConfigUnstableClient()
			if err != nil {
				return err
			}

			streamer := output.NewWriteObjectStreamer[*LinkTemplate](writer)
			nextToken, err := ListLinkTemplates(ctx, client, streamer, listOptions)
			if err != nil {
				return err
			}

			if nextToken != "" {
				nextPage := pagination.Result{
					Kind:          pagination.ResultKind,
					Message:       "There are additional linkTemplates. To view more, use the next page token or run the full command.",
					NextPageToken: nextToken,
					FullCommand: fmt.Sprintf("%s --page-token %q",
						cli.BuildCommandString(cmd, []string{"page-token"}),
						nextToken),
				}
				if err := outputFlags.WriteObject(nextPage, cmd.OutOrStdout()); err != nil {
					return err
				}
			}

			return nil
		},
	}

	listOptions.registerFlags(cmd.Flags())
	clientFlags.AddFlags(cmd)
	outputFlags.AddFlags(cmd)

	return cmd
}

const LinkTemplateScaffoldYAML = `api_version: unstable/config
kind: LinkTemplate
spec:
    # Unique slug identifying the LinkTemplate.
    slug: <string>
    # Unique name of the LinkTemplate. Used as the link text.
    name: <string>
    # URL template.
    url_template: <string>
    # Link template is scoped to the specified string representing a UI component here.
    ui_component: <string>
`

func newLinkTemplateScaffoldCmd() *cobra.Command {
	cmd := &cobra.Command{
		Use:     "scaffold",
		GroupID: groups.Commands.ID,
		Short:   "Scaffolds a complete object with placeholder values",
		Run: func(cmd *cobra.Command, args []string) {
			fmt.Fprint(cmd.OutOrStdout(), LinkTemplateScaffoldYAML)
		},
	}
	return cmd
}

func NewLinkTemplateCmd() *cobra.Command {
	root := &cobra.Command{
		Use:     "link-templates",
		GroupID: groups.Config.ID,
		Short:   "All commands for LinkTemplates",
	}

	root.AddGroup(groups.Commands)
	root.AddCommand(
		newLinkTemplateCreateCmd(),
		newLinkTemplateReadCmd(),
		newLinkTemplateUpdateCmd(),
		newLinkTemplateDeleteCmd(),
		newLinkTemplateListCmd(),
		newLinkTemplateScaffoldCmd(),
	)
	return root
}
