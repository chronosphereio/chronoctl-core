// Code generated by chronogen; DO NOT EDIT
package configunstable

import (
	"context"
	"fmt"

	"github.com/chronosphereio/chronoctl-core/src/cmd/pkg/client"
	"github.com/chronosphereio/chronoctl-core/src/cmd/pkg/clienterror"
	"github.com/chronosphereio/chronoctl-core/src/cmd/pkg/dry"
	"github.com/chronosphereio/chronoctl-core/src/cmd/pkg/file"
	"github.com/chronosphereio/chronoctl-core/src/cmd/pkg/groups"
	"github.com/chronosphereio/chronoctl-core/src/cmd/pkg/output"
	config_unstable "github.com/chronosphereio/chronoctl-core/src/generated/swagger/configunstable/client/operations"
	"github.com/chronosphereio/chronoctl-core/src/generated/swagger/configunstable/models"
	"github.com/chronosphereio/chronoctl-core/src/types"
	"github.com/spf13/cobra"
)

func init() { types.MustRegisterObject(QueryResourcePoolsTypeMeta, &QueryResourcePools{}) }

var _ types.Object = &QueryResourcePools{}

var QueryResourcePoolsTypeMeta = types.TypeMeta{
	APIVersion: "unstable/config",
	Kind:       "QueryResourcePools",
}

type QueryResourcePools struct {
	types.TypeMeta `json:",inline"`
	Spec           *models.ConfigunstableQueryResourcePools `json:"spec"`
}

func NewQueryResourcePools(spec *models.ConfigunstableQueryResourcePools) *QueryResourcePools {
	return &QueryResourcePools{
		TypeMeta: QueryResourcePoolsTypeMeta,
		Spec:     spec,
	}
}

func (e *QueryResourcePools) Description() string {
	return types.TypeDescription(e)
}

func (e *QueryResourcePools) Identifier() string {
	return "QueryResourcePools"
}

func CreateQueryResourcePools(
	ctx context.Context,
	client config_unstable.ClientService,
	entity *QueryResourcePools,
	dryRun bool,
) (*QueryResourcePools, error) {
	res, err := client.CreateQueryResourcePools(&config_unstable.CreateQueryResourcePoolsParams{
		Context: ctx,
		Body: &models.ConfigunstableCreateQueryResourcePoolsRequest{
			DryRun:             dryRun,
			QueryResourcePools: entity.Spec,
		},
	})
	if err != nil {
		return nil, clienterror.Wrap(err)
	}
	return NewQueryResourcePools(res.Payload.QueryResourcePools), nil
}

func newQueryResourcePoolsCreateCmd() *cobra.Command {
	var (
		permissiveParsing bool
		dryRunFlags       = dry.NewFlags()
		clientFlags       = client.NewClientFlags()
		outputFlags       = output.NewFlags(output.WithoutOutputDirectory(), output.WithoutCreateFilePerObject())
		fileFlags         = file.NewFlags(true /* required */)
	)

	var (
		use   string
		short string
	)
	use = "create -f <file>"
	short = "Creates a single QueryResourcePools."

	cmd := &cobra.Command{
		Use:     use,
		GroupID: groups.Commands.ID,
		Short:   short,
		RunE: func(cmd *cobra.Command, args []string) error {
			ctx, cancel := context.WithTimeout(cmd.Context(), clientFlags.Timeout())
			defer cancel()
			if err := outputFlags.Validate(); err != nil {
				return err
			}
			defer outputFlags.Close(cmd.OutOrStdout())
			stderr := output.NewStderrPrinter(cmd)

			client, err := clientFlags.ConfigUnstableClient()
			if err != nil {
				return err
			}

			var queryResourcePools *QueryResourcePools
			file, err := fileFlags.File()
			if err != nil {
				return err
			}
			defer file.Close() //nolint:errcheck
			queryResourcePools, err = types.MustDecodeSingleObject[*QueryResourcePools](file, permissiveParsing)
			if err != nil {
				return err
			}

			if dryRunFlags.DryRun {
				stderr.Println("--dry-run is set")
			}
			fullQueryResourcePools, err := CreateQueryResourcePools(ctx, client, queryResourcePools, dryRunFlags.DryRun)
			if err != nil {
				return err
			}

			if dryRunFlags.DryRun {
				stderr.Println("QueryResourcePools is valid and can be created")
				return nil
			}
			stderr.Printf("QueryResourcePools created successfully\n")

			if err := outputFlags.WriteObject(fullQueryResourcePools, cmd.OutOrStdout()); err != nil {
				return err
			}
			return nil
		},
	}
	dryRunFlags.AddFlags(cmd)
	clientFlags.AddFlags(cmd)
	outputFlags.AddFlags(cmd)
	fileFlags.AddFlags(cmd)
	cmd.Flags().BoolVar(&permissiveParsing, "no-strict", false, "If set, manifests with unknown fields are not rejected. Defaults to false.")

	return cmd
}

func GetQueryResourcePools(
	ctx context.Context,
	client config_unstable.ClientService,
) (*QueryResourcePools, error) {
	res, err := client.ReadQueryResourcePools(&config_unstable.ReadQueryResourcePoolsParams{
		Context: ctx,
	})
	if err != nil {
		return nil, clienterror.Wrap(err)
	}
	return NewQueryResourcePools(res.GetPayload().QueryResourcePools), nil
}

func newQueryResourcePoolsReadCmd() *cobra.Command {
	clientFlags := client.NewClientFlags()
	outputFlags := output.NewFlags(output.WithoutOutputDirectory(), output.WithoutCreateFilePerObject())
	var (
		short string
		use   string
		args  cobra.PositionalArgs
	)
	short = "Reads a QueryResourcePools singleton"
	use = "read"

	cmd := &cobra.Command{
		Use:     use,
		GroupID: groups.Commands.ID,
		Short:   short,
		Args:    args,
		RunE: func(cmd *cobra.Command, args []string) error {
			ctx, cancel := context.WithTimeout(cmd.Context(), clientFlags.Timeout())
			defer cancel()
			if err := outputFlags.Validate(); err != nil {
				return err
			}
			defer outputFlags.Close(cmd.OutOrStdout())

			client, err := clientFlags.ConfigUnstableClient()
			if err != nil {
				return err
			}
			entity, err := GetQueryResourcePools(ctx, client)
			if err != nil {
				return err
			}
			if err := outputFlags.WriteObject(entity, cmd.OutOrStdout()); err != nil {
				return err
			}
			return nil
		},
	}

	clientFlags.AddFlags(cmd)
	outputFlags.AddFlags(cmd)

	return cmd
}

func UpdateQueryResourcePools(
	ctx context.Context,
	client config_unstable.ClientService,
	entity *QueryResourcePools,
	opts UpdateOptions,
) (*QueryResourcePools, error) {
	res, err := client.UpdateQueryResourcePools(&config_unstable.UpdateQueryResourcePoolsParams{
		Context: ctx,
		Body: &models.ConfigunstableUpdateQueryResourcePoolsRequest{
			CreateIfMissing:    opts.CreateIfMissing,
			DryRun:             opts.DryRun,
			QueryResourcePools: entity.Spec,
		},
	})
	if err != nil {
		return nil, clienterror.Wrap(err)
	}

	return NewQueryResourcePools(res.Payload.QueryResourcePools), nil
}

func newQueryResourcePoolsUpdateCmd() *cobra.Command {
	var (
		permissiveParsing bool
		createIfMissing   bool
		dryRunFlags       = dry.NewFlags()
		clientFlags       = client.NewClientFlags()
		outputFlags       = output.NewFlags(output.WithoutOutputDirectory(), output.WithoutCreateFilePerObject())
		fileFlags         = file.NewFlags(true /* required */)
	)

	cmd := &cobra.Command{
		Use:     "update -f <filename>",
		GroupID: groups.Commands.ID,
		Short:   "Updates an existing QueryResourcePools.",
		RunE: func(cmd *cobra.Command, args []string) error {
			ctx, cancel := context.WithTimeout(cmd.Context(), clientFlags.Timeout())
			defer cancel()
			if err := outputFlags.Validate(); err != nil {
				return err
			}
			defer outputFlags.Close(cmd.OutOrStdout())
			stderr := output.NewStderrPrinter(cmd)

			client, err := clientFlags.ConfigUnstableClient()
			if err != nil {
				return err
			}

			file, err := fileFlags.File()
			if err != nil {
				return err
			}
			defer file.Close() //nolint:errcheck

			queryResourcePools, err := types.MustDecodeSingleObject[*QueryResourcePools](file, permissiveParsing)
			if err != nil {
				return err
			}

			updateOpts := UpdateOptions{
				DryRun:          dryRunFlags.DryRun,
				CreateIfMissing: createIfMissing,
			}

			if dryRunFlags.DryRun {
				stderr.Println("--dry-run is set, update not persisted")
			}

			fullQueryResourcePools, err := UpdateQueryResourcePools(ctx, client, queryResourcePools, updateOpts)
			if err != nil {
				return err
			}

			if dryRunFlags.DryRun {
				stderr.Println("QueryResourcePools is valid and can be updated")
				return nil
			}
			stderr.Printf("QueryResourcePools applied successfully\n")

			if err := outputFlags.WriteObject(fullQueryResourcePools, cmd.OutOrStdout()); err != nil {
				return err
			}
			return nil
		},
	}
	dryRunFlags.AddFlags(cmd)
	clientFlags.AddFlags(cmd)
	outputFlags.AddFlags(cmd)
	fileFlags.AddFlags(cmd)
	cmd.Flags().BoolVar(&permissiveParsing, "no-strict", false, "If set, manifests with unknown fields are allowed. Defaults to false.")
	cmd.Flags().BoolVar(&createIfMissing, "create-if-missing", false, "If set, creates the QueryResourcePools if it does not already exist. Defaults to false.")

	return cmd
}

func DeleteQueryResourcePools(
	ctx context.Context,
	client config_unstable.ClientService,
) error {
	_, err := client.DeleteQueryResourcePools(&config_unstable.DeleteQueryResourcePoolsParams{
		Context: ctx,
	})
	if err != nil {
		return clienterror.Wrap(err)
	}
	return nil
}

func newQueryResourcePoolsDeleteCmd() *cobra.Command {
	clientFlags := client.NewClientFlags()
	outputFlags := output.NewFlags(output.WithoutOutputDirectory(), output.WithoutCreateFilePerObject())

	cmd := &cobra.Command{
		Use:     "delete",
		GroupID: groups.Commands.ID,
		Short:   "Deletes the QueryResourcePools singleton",
		Args:    cobra.NoArgs,
		RunE: func(cmd *cobra.Command, args []string) error {
			ctx, cancel := context.WithTimeout(cmd.Context(), clientFlags.Timeout())
			defer cancel()
			if err := outputFlags.Validate(); err != nil {
				return err
			}
			defer outputFlags.Close(cmd.OutOrStdout())

			client, err := clientFlags.ConfigUnstableClient()
			if err != nil {
				return err
			}

			res, err := client.DeleteQueryResourcePools(&config_unstable.DeleteQueryResourcePoolsParams{
				Context: ctx,
			})
			if err != nil {
				return clienterror.Wrap(err)
			}
			_ = res
			fmt.Fprintf(cmd.OutOrStdout(), "deleted QueryResourcePools")
			return nil
		},
	}
	clientFlags.AddFlags(cmd)
	outputFlags.AddFlags(cmd)
	return cmd
}

const QueryResourcePoolsScaffoldYAML = `api_version: unstable/config
kind: QueryResourcePool
spec:
    # Optional. Pools that group automated query sources. Query sources map to
    # the first pool that matches, in configured order.
    pools:
        - # Name of the pool, which must be unique.
          name: <string>
          # Source types that map to this pool. A query source maps to the first
          # pool, in configured order, whose 'source_types' matches its source
          # type and whose 'source_names' (if set) matches its source name.
          source_types:
            - <MONITOR|RECORDING_RULE|SLO|SERVICE_ACCOUNT>
          # Optional. Narrows the pool to specific named sources of the pool's
          # source types: service account IDs for 'SERVICE_ACCOUNT', monitor slugs
          # for 'MONITOR', recording rule slugs for 'RECORDING_RULE', and SLO
          # slugs for 'SLO'.
          source_names:
            - <string>
          # DataReadLimit defines the datapoints-read rate limit for a pool and the
          # circuit-breaker behavior applied when the pool exceeds it. If a pool has
          # no 'data_read_limit', its consumption is measured but never limited,
          # subject only to the tenant-wide query capacity system limits.
          data_read_limit:
            # Maximum datapoints read per second for the pool, aggregated across all
            # sources in the pool. This field must be set explicitly; a value of '0'
            # blocks all queries from the pool.
            max_datapoints_read_per_second: <int64>
            # Optional. How long the pool must remain above
            # 'max_datapoints_read_per_second' before its queries are rejected. Must
            # be a whole multiple of 60 seconds, between 60 and 300; '0' (or
            # omitted) applies the 60-second default. Must be omitted when
            # 'max_datapoints_read_per_second' is '0', where queries are rejected
            # immediately.

            # Once a pool trips its limit, all of its queries are rejected for a
            # penalty period, which increases on repeated offenses and resets after
            # the pool remains under its limit. The penalty behavior is
            # system-managed and not configurable.
            sustain_secs: <integer>
    default_pool:
        # DataReadLimit defines the datapoints-read rate limit for a pool and the
        # circuit-breaker behavior applied when the pool exceeds it. If a pool has
        # no 'data_read_limit', its consumption is measured but never limited,
        # subject only to the tenant-wide query capacity system limits.
        data_read_limit:
            # Maximum datapoints read per second for the pool, aggregated across all
            # sources in the pool. This field must be set explicitly; a value of '0'
            # blocks all queries from the pool.
            max_datapoints_read_per_second: <int64>
            # Optional. How long the pool must remain above
            # 'max_datapoints_read_per_second' before its queries are rejected. Must
            # be a whole multiple of 60 seconds, between 60 and 300; '0' (or
            # omitted) applies the 60-second default. Must be omitted when
            # 'max_datapoints_read_per_second' is '0', where queries are rejected
            # immediately.

            # Once a pool trips its limit, all of its queries are rejected for a
            # penalty period, which increases on repeated offenses and resets after
            # the pool remains under its limit. The penalty behavior is
            # system-managed and not configurable.
            sustain_secs: <integer>
`

func newQueryResourcePoolsScaffoldCmd() *cobra.Command {
	cmd := &cobra.Command{
		Use:     "scaffold",
		GroupID: groups.Commands.ID,
		Short:   "Scaffolds a complete object with placeholder values",
		Run: func(cmd *cobra.Command, args []string) {
			fmt.Fprint(cmd.OutOrStdout(), QueryResourcePoolsScaffoldYAML)
		},
	}
	return cmd
}

func NewQueryResourcePoolsCmd() *cobra.Command {
	root := &cobra.Command{
		Use:     "query-resource-pools",
		GroupID: groups.Config.ID,
		Short:   "All commands for QueryResourcePools",
	}

	root.AddGroup(groups.Commands)
	root.AddCommand(
		newQueryResourcePoolsCreateCmd(),
		newQueryResourcePoolsReadCmd(),
		newQueryResourcePoolsUpdateCmd(),
		newQueryResourcePoolsDeleteCmd(),
		newQueryResourcePoolsScaffoldCmd(),
	)
	return root
}
