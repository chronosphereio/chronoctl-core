// Code generated by chronogen; DO NOT EDIT
package configv1

import (
	"context"
	"fmt"

	"github.com/chronosphereio/chronoctl-core/src/cmd/cli"
	"github.com/chronosphereio/chronoctl-core/src/cmd/pkg/client"
	"github.com/chronosphereio/chronoctl-core/src/cmd/pkg/clienterror"
	"github.com/chronosphereio/chronoctl-core/src/cmd/pkg/dry"
	"github.com/chronosphereio/chronoctl-core/src/cmd/pkg/file"
	"github.com/chronosphereio/chronoctl-core/src/cmd/pkg/groups"
	"github.com/chronosphereio/chronoctl-core/src/cmd/pkg/output"
	"github.com/chronosphereio/chronoctl-core/src/cmd/pkg/pagination"
	"github.com/chronosphereio/chronoctl-core/src/cmd/pkg/ptr"
	config_v1 "github.com/chronosphereio/chronoctl-core/src/generated/swagger/configv1/client/operations"
	"github.com/chronosphereio/chronoctl-core/src/generated/swagger/configv1/models"
	"github.com/chronosphereio/chronoctl-core/src/types"
	"github.com/spf13/cobra"
	flag "github.com/spf13/pflag"
)

func init() { types.MustRegisterObject(SyntheticTestTypeMeta, &SyntheticTest{}) }

var _ types.Object = &SyntheticTest{}

var SyntheticTestTypeMeta = types.TypeMeta{
	APIVersion: "v1/config",
	Kind:       "SyntheticTest",
}

type SyntheticTest struct {
	types.TypeMeta `json:",inline"`
	Spec           *models.Configv1SyntheticTest `json:"spec"`
}

func NewSyntheticTest(spec *models.Configv1SyntheticTest) *SyntheticTest {
	return &SyntheticTest{
		TypeMeta: SyntheticTestTypeMeta,
		Spec:     spec,
	}
}

func (e *SyntheticTest) Description() string {
	return types.TypeDescription(e, "name", e.Spec.Name, "slug", e.Spec.Slug)
}

func (e *SyntheticTest) Identifier() string {
	return e.Spec.Slug
}

func CreateSyntheticTest(
	ctx context.Context,
	client config_v1.ClientService,
	entity *SyntheticTest,
	dryRun bool,
) (*SyntheticTest, error) {
	res, err := client.CreateSyntheticTest(&config_v1.CreateSyntheticTestParams{
		Context: ctx,
		Body: &models.Configv1CreateSyntheticTestRequest{
			DryRun:        dryRun,
			SyntheticTest: entity.Spec,
		},
	})
	if err != nil {
		return nil, clienterror.Wrap(err)
	}
	return NewSyntheticTest(res.Payload.SyntheticTest), nil
}

func newSyntheticTestCreateCmd() *cobra.Command {
	var (
		permissiveParsing bool
		dryRunFlags       = dry.NewFlags()
		clientFlags       = client.NewClientFlags()
		outputFlags       = output.NewFlags(output.WithoutOutputDirectory(), output.WithoutCreateFilePerObject())
		fileFlags         = file.NewFlags(true /* required */)
	)

	var (
		use   string
		short string
	)
	use = "create -f <file>"
	short = "Creates a single SyntheticTest."

	cmd := &cobra.Command{
		Use:     use,
		GroupID: groups.Commands.ID,
		Short:   short,
		RunE: func(cmd *cobra.Command, args []string) error {
			ctx, cancel := context.WithTimeout(cmd.Context(), clientFlags.Timeout())
			defer cancel()
			if err := outputFlags.Validate(); err != nil {
				return err
			}
			defer outputFlags.Close(cmd.OutOrStdout())
			stderr := output.NewStderrPrinter(cmd)

			client, err := clientFlags.ConfigV1Client()
			if err != nil {
				return err
			}

			var syntheticTest *SyntheticTest
			file, err := fileFlags.File()
			if err != nil {
				return err
			}
			defer file.Close() //nolint:errcheck
			syntheticTest, err = types.MustDecodeSingleObject[*SyntheticTest](file, permissiveParsing)
			if err != nil {
				return err
			}

			if dryRunFlags.DryRun {
				stderr.Println("--dry-run is set")
			}
			fullSyntheticTest, err := CreateSyntheticTest(ctx, client, syntheticTest, dryRunFlags.DryRun)
			if err != nil {
				return err
			}

			if dryRunFlags.DryRun {
				stderr.Println("SyntheticTest is valid and can be created")
				return nil
			}
			stderr.Printf("SyntheticTest with slug %q created successfully\n", fullSyntheticTest.Spec.Slug)

			if err := outputFlags.WriteObject(fullSyntheticTest, cmd.OutOrStdout()); err != nil {
				return err
			}
			return nil
		},
	}
	dryRunFlags.AddFlags(cmd)
	clientFlags.AddFlags(cmd)
	outputFlags.AddFlags(cmd)
	fileFlags.AddFlags(cmd)
	cmd.Flags().BoolVar(&permissiveParsing, "no-strict", false, "If set, manifests with unknown fields are not rejected. Defaults to false.")

	return cmd
}

func GetSyntheticTest(
	ctx context.Context,
	client config_v1.ClientService,
	slug string,
) (*SyntheticTest, error) {
	res, err := client.ReadSyntheticTest(&config_v1.ReadSyntheticTestParams{
		Context: ctx,
		Slug:    slug,
	})
	if err != nil {
		return nil, clienterror.Wrap(err)
	}
	return NewSyntheticTest(res.GetPayload().SyntheticTest), nil
}

func newSyntheticTestReadCmd() *cobra.Command {
	clientFlags := client.NewClientFlags()
	outputFlags := output.NewFlags(output.WithoutOutputDirectory(), output.WithoutCreateFilePerObject())
	var (
		short string
		use   string
		args  cobra.PositionalArgs
	)
	short = "Reads a single SyntheticTest by slug"
	use = "read <slug>"
	args = cobra.ExactArgs(1)

	cmd := &cobra.Command{
		Use:     use,
		GroupID: groups.Commands.ID,
		Short:   short,
		Args:    args,
		RunE: func(cmd *cobra.Command, args []string) error {
			ctx, cancel := context.WithTimeout(cmd.Context(), clientFlags.Timeout())
			defer cancel()
			if err := outputFlags.Validate(); err != nil {
				return err
			}
			defer outputFlags.Close(cmd.OutOrStdout())

			client, err := clientFlags.ConfigV1Client()
			if err != nil {
				return err
			}
			entity, err := GetSyntheticTest(ctx, client, args[0])
			if err != nil {
				return err
			}
			if err := outputFlags.WriteObject(entity, cmd.OutOrStdout()); err != nil {
				return err
			}
			return nil
		},
	}

	clientFlags.AddFlags(cmd)
	outputFlags.AddFlags(cmd)

	return cmd
}

func UpdateSyntheticTest(
	ctx context.Context,
	client config_v1.ClientService,
	entity *SyntheticTest,
	opts UpdateOptions,
) (*SyntheticTest, error) {
	res, err := client.UpdateSyntheticTest(&config_v1.UpdateSyntheticTestParams{
		Context: ctx,
		Slug:    entity.Spec.Slug,
		Body: &models.ConfigV1UpdateSyntheticTestBody{
			CreateIfMissing: opts.CreateIfMissing,
			DryRun:          opts.DryRun,
			SyntheticTest:   entity.Spec,
		},
	})
	if err != nil {
		return nil, clienterror.Wrap(err)
	}

	return NewSyntheticTest(res.Payload.SyntheticTest), nil
}

func newSyntheticTestUpdateCmd() *cobra.Command {
	var (
		permissiveParsing bool
		createIfMissing   bool
		dryRunFlags       = dry.NewFlags()
		clientFlags       = client.NewClientFlags()
		outputFlags       = output.NewFlags(output.WithoutOutputDirectory(), output.WithoutCreateFilePerObject())
		fileFlags         = file.NewFlags(true /* required */)
	)

	cmd := &cobra.Command{
		Use:     "update -f <filename>",
		GroupID: groups.Commands.ID,
		Short:   "Updates an existing SyntheticTest.",
		RunE: func(cmd *cobra.Command, args []string) error {
			ctx, cancel := context.WithTimeout(cmd.Context(), clientFlags.Timeout())
			defer cancel()
			if err := outputFlags.Validate(); err != nil {
				return err
			}
			defer outputFlags.Close(cmd.OutOrStdout())
			stderr := output.NewStderrPrinter(cmd)

			client, err := clientFlags.ConfigV1Client()
			if err != nil {
				return err
			}

			file, err := fileFlags.File()
			if err != nil {
				return err
			}
			defer file.Close() //nolint:errcheck

			syntheticTest, err := types.MustDecodeSingleObject[*SyntheticTest](file, permissiveParsing)
			if err != nil {
				return err
			}

			updateOpts := UpdateOptions{
				DryRun:          dryRunFlags.DryRun,
				CreateIfMissing: createIfMissing,
			}

			if dryRunFlags.DryRun {
				stderr.Println("--dry-run is set, update not persisted")
			}

			fullSyntheticTest, err := UpdateSyntheticTest(ctx, client, syntheticTest, updateOpts)
			if err != nil {
				return err
			}

			if dryRunFlags.DryRun {
				stderr.Println("SyntheticTest is valid and can be updated")
				return nil
			}
			stderr.Printf("SyntheticTest with slug %q applied successfully\n", fullSyntheticTest.Spec.Slug)

			if err := outputFlags.WriteObject(fullSyntheticTest, cmd.OutOrStdout()); err != nil {
				return err
			}
			return nil
		},
	}
	dryRunFlags.AddFlags(cmd)
	clientFlags.AddFlags(cmd)
	outputFlags.AddFlags(cmd)
	fileFlags.AddFlags(cmd)
	cmd.Flags().BoolVar(&permissiveParsing, "no-strict", false, "If set, manifests with unknown fields are allowed. Defaults to false.")
	cmd.Flags().BoolVar(&createIfMissing, "create-if-missing", false, "If set, creates the SyntheticTest if it does not already exist. Defaults to false.")

	return cmd
}

func DeleteSyntheticTest(
	ctx context.Context,
	client config_v1.ClientService,
	slug string,
) error {
	_, err := client.DeleteSyntheticTest(&config_v1.DeleteSyntheticTestParams{
		Context: ctx,
		Slug:    slug,
	})
	if err != nil {
		return clienterror.Wrap(err)
	}
	return nil
}

func newSyntheticTestDeleteCmd() *cobra.Command {
	clientFlags := client.NewClientFlags()
	outputFlags := output.NewFlags(output.WithoutOutputDirectory(), output.WithoutCreateFilePerObject())

	cmd := &cobra.Command{
		Use:     "delete <slug>",
		GroupID: groups.Commands.ID,
		Short:   "Deletes a single SyntheticTest by slug",
		Args:    cobra.ExactArgs(1),
		RunE: func(cmd *cobra.Command, args []string) error {
			ctx, cancel := context.WithTimeout(cmd.Context(), clientFlags.Timeout())
			defer cancel()
			if err := outputFlags.Validate(); err != nil {
				return err
			}
			defer outputFlags.Close(cmd.OutOrStdout())

			client, err := clientFlags.ConfigV1Client()
			if err != nil {
				return err
			}

			res, err := client.DeleteSyntheticTest(&config_v1.DeleteSyntheticTestParams{
				Context: ctx,
				Slug:    args[0],
			})
			if err != nil {
				return clienterror.Wrap(err)
			}
			_ = res
			fmt.Fprintf(cmd.OutOrStdout(), "deleted SyntheticTest with slug %q\n", args[0])

			return nil
		},
	}
	clientFlags.AddFlags(cmd)
	outputFlags.AddFlags(cmd)
	return cmd
}

type SyntheticTestListOpts struct {
	// Limit represents that maximum number of items we wish to return.
	Limit int
	// PageToken is the pagination token we want to start our request at.
	PageToken string
	// PageMaxSize is the maximum page size to use when making List calls.
	PageMaxSize     int
	CollectionSlugs []string
	Names           []string
	Slugs           []string
	Statuses        []string
	TeamSlugs       []string
	TestTypes       []string
}

func (r *SyntheticTestListOpts) registerFlags(flags *flag.FlagSet) {
	var emptyCollectionSlugs []string
	flags.StringSliceVar(&r.CollectionSlugs, "collection-slugs", emptyCollectionSlugs, "Filter returned tests by the collections that own them.")
	var emptyNames []string
	flags.StringSliceVar(&r.Names, "names", emptyNames, "Filters results by name, where any SyntheticTest with a matching name in the given list (and matches all other filters) will be returned.")
	var emptySlugs []string
	flags.StringSliceVar(&r.Slugs, "slugs", emptySlugs, "Filters results by slug, where any SyntheticTest with a matching slug in the given list (and matches all other filters) will be returned.")
	var emptyStatuses []string
	flags.StringSliceVar(&r.Statuses, "statuses", emptyStatuses, "")
	var emptyTeamSlugs []string
	flags.StringSliceVar(&r.TeamSlugs, "team-slugs", emptyTeamSlugs, "Filter returned tests by the teams that own the collections they belong to.")
	var emptyTestTypes []string
	flags.StringSliceVar(&r.TestTypes, "test-types", emptyTestTypes, "Filters results by test_type, where any SyntheticTest with a matching test_type in the given list (and matches all other filters) will be returned.")
	flags.IntVar(&r.Limit, "limit", 0, "maximum number of items to return")
	flags.IntVar(&r.PageMaxSize, "page-max-size", 0, "maximum page size")
	flags.StringVar(&r.PageToken, "page-token", "", "begins listing items at the start of the pagination token")
}

func ListSyntheticTests(
	ctx context.Context,
	client config_v1.ClientService,
	streamer output.Streamer[*SyntheticTest],
	opts SyntheticTestListOpts,
) (pagination.Token, error) {
	var (
		gotItems    = 0
		nextToken   = opts.PageToken
		pageMaxSize = opts.PageMaxSize
	)

	// Use the limit if it's set, and smaller than a set page size.
	if opts.Limit > 0 && (opts.Limit < pageMaxSize || pageMaxSize == 0) {
		pageMaxSize = opts.Limit
	}

	for {
		res, err := client.ListSyntheticTests(&config_v1.ListSyntheticTestsParams{
			Context:         ctx,
			PageToken:       &nextToken,
			PageMaxSize:     ptr.Int64(int64(pageMaxSize)),
			CollectionSlugs: opts.CollectionSlugs,
			Names:           opts.Names,
			Slugs:           opts.Slugs,
			Statuses:        opts.Statuses,
			TeamSlugs:       opts.TeamSlugs,
			TestTypes:       opts.TestTypes,
		})
		if err != nil {
			return pagination.Token(""), clienterror.Wrap(err)
		}

		for _, v := range res.Payload.SyntheticTests {
			if err := streamer(NewSyntheticTest(v)); err != nil {
				return pagination.Token(""), err
			}
			gotItems++
		}

		nextToken = res.Payload.Page.NextToken
		if nextToken == "" {
			return pagination.Token(""), nil
		}

		if opts.Limit > 0 && gotItems >= opts.Limit {
			return pagination.Token(nextToken), nil
		}

		pageMaxSize = pagination.CalculatePageSize(pagination.Calculation{
			GotItems:    gotItems,
			MaxItems:    opts.Limit,
			MaxPageSize: len(res.Payload.SyntheticTests),
		})
	}
}

func newSyntheticTestListCmd() *cobra.Command {
	var listOptions SyntheticTestListOpts
	clientFlags := client.NewClientFlags()
	outputFlags := output.NewFlags()

	cmd := &cobra.Command{
		Use:     "list",
		Short:   "Lists all SyntheticTests and applies optional filters",
		GroupID: groups.Commands.ID,
		RunE: func(cmd *cobra.Command, args []string) error {
			ctx, cancel := context.WithTimeout(cmd.Context(), clientFlags.Timeout())
			defer cancel()
			if err := outputFlags.Validate(); err != nil {
				return err
			}

			writer, err := outputFlags.NewWriterManager(cmd.OutOrStdout())
			if err != nil {
				return err
			}
			defer writer.Close()

			client, err := clientFlags.ConfigV1Client()
			if err != nil {
				return err
			}

			streamer := output.NewWriteObjectStreamer[*SyntheticTest](writer)
			nextToken, err := ListSyntheticTests(ctx, client, streamer, listOptions)
			if err != nil {
				return err
			}

			if nextToken != "" {
				nextPage := pagination.Result{
					Kind:          pagination.ResultKind,
					Message:       "There are additional syntheticTests. To view more, use the next page token or run the full command.",
					NextPageToken: nextToken,
					FullCommand: fmt.Sprintf("%s --page-token %q",
						cli.BuildCommandString(cmd, []string{"page-token"}),
						nextToken),
				}
				if err := outputFlags.WriteObject(nextPage, cmd.OutOrStdout()); err != nil {
					return err
				}
			}

			return nil
		},
	}

	listOptions.registerFlags(cmd.Flags())
	clientFlags.AddFlags(cmd)
	outputFlags.AddFlags(cmd)

	return cmd
}

const SyntheticTestScaffoldYAML = `api_version: v1/config
kind: SyntheticTest
spec:
    # The name of the SyntheticTest. You can modify this value after the SyntheticTest is created.
    name: <string>
    # The unique identifier of the SyntheticTest. If a 'slug' isn't provided, one is generated based on the 'name' field. You can't modify this field after the SyntheticTest is created.
    slug: <string>
    interval_secs: <integer>
    locations:
        - <GCP_US_OREGON|GCP_US_VIRGINIA|GCP_BR_SAO_PAULO|GCP_SG_SINGAPORE|GCP_AU_SYDNEY|GCP_IN_MUMBAI|GCP_JP_TOKYO|GCP_GB_LONDON|GCP_DE_FRANKFURT|GCP_ZA_JOHANNESBURG|GCP_ID_JAKARTA|GCP_CA_MONTREAL|GCP_KR_SEOUL|GCP_IT_MILAN|GCP_QA_DOHA|GCP_JP_OSAKA|GCP_FR_PARIS>
    description: <string>
    labels:
        key_1: <string>
    # optional — 1-60s; defaults to 60s
    timeout_secs: <integer>
    # Slug of the collection that owns the test. Required if 'collection' isn't
    # set.
    collection_slug: <string>
    collection:
        slug: <string>
        type: <SIMPLE|SERVICE>
    dns_test:
        domain: <string>
        # Optional explicit nameserver to query; runner default resolver if unset.
        dns_server: <string>
        # Nameserver port; required when dns_server is set, rejected without it.
        dns_server_port: <integer>
        assertions:
            - # Asserts on DNS records returned for the query. match_scope selects whether
              # the operator must hold for every record of record_type or for at least
              # one. SOA and SRV record types are only valid with AT_LEAST_ONE.
              dns_record_assertion:
                target: <string>
                match_scope: <EVERY_AVAILABLE|AT_LEAST_ONE>
                operator: <CONTAINS|EQUALS|REGEX_EQUALS|REGEX_NOT_EQUALS>
                record_type: <A|AAAA|CNAME|MX|NS|TXT|SOA|SRV>
              # Asserts that the request response time is below the target, in
              # milliseconds. Scope selects whether DNS resolution time is included.
              response_time_assertion:
                target_ms: <integer>
                operator: <LESS_THAN>
                scope: <INCLUDING_DNS|WITHOUT_DNS>
    http_test:
        url: <string>
        headers:
            - name: <string>
              value: <string>
        request_body: <byte>
        query_params:
            key_1: <string>
        follow_redirects: <true|false>
        # optional — 0-10; defaults to 10 when follow_redirects is set
        max_redirects: <integer>
        assertions:
            - # Asserts that the raw response body matches the target string.
              body_assertion:
                target: <string>
                operator: <CONTAINS|NOT_CONTAINS|EQUALS|NOT_EQUALS|REGEX_EQUALS|REGEX_NOT_EQUALS>
              # Asserts that a hash of the response body equals the target.
              body_hash_assertion:
                target: <string>
                algorithm: <MD5|SHA1|SHA256>
              # Asserts that a value extracted via JSONPath matches the target.
              # match_type controls how multiple matches are evaluated.
              body_json_path_assertion:
                json_path: <string>
                target: <string>
                match_type: <FIRST_ELEMENT|EVERY_ELEMENT|AT_LEAST_ONE_ELEMENT|SERIALIZATION>
                operator: <CONTAINS|NOT_CONTAINS|EQUALS|NOT_EQUALS|REGEX_EQUALS|REGEX_NOT_EQUALS|LESS_THAN|GREATER_THAN|IS_UNDEFINED>
              # Asserts that the JSON response body conforms to a JSON schema.
              body_json_schema_assertion:
                schema: <string>
                draft: <DRAFT_06|DRAFT_07>
              # Asserts that a value extracted via XPath 1.0 matches the target.
              body_xpath_assertion:
                xpath: <string>
                target: <string>
                operator: <CONTAINS|NOT_CONTAINS|EQUALS|NOT_EQUALS|REGEX_EQUALS|REGEX_NOT_EQUALS|LESS_THAN|GREATER_THAN|IS_UNDEFINED>
              # Asserts that the named response header satisfies the operator.
              header_assertion:
                name: <string>
                target: <string>
                operator: <CONTAINS|NOT_CONTAINS|EQUALS|NOT_EQUALS|REGEX_EQUALS|REGEX_NOT_EQUALS|NOT_EXISTS|LESS_THAN|LESS_THAN_OR_EQUAL|GREATER_THAN|GREATER_THAN_OR_EQUAL>
              # Asserts that the request response time is below the target, in
              # milliseconds. Scope selects whether DNS resolution time is included.
              response_time_assertion:
                target_ms: <integer>
                operator: <LESS_THAN>
                scope: <INCLUDING_DNS|WITHOUT_DNS>
              # Asserts that the HTTP response status code matches the target.
              # 'target' is a string so regex patterns (e.g. "2..") can share the field
              # with literal codes (e.g. "200").
              status_code_assertion:
                target: <string>
                operator: <EQUALS|NOT_EQUALS|REGEX_EQUALS|REGEX_NOT_EQUALS>
        # Skip TLS certificate verification for HTTPS requests.
        allow_insecure_tls: <true|false>
        # Cap on captured response-body size in bytes; 0 means use the default.
        max_response_body_bytes: <integer>
        # When set, the captured body is not persisted on failed executions.
        do_not_save_response_body_on_failure: <true|false>
        cookies:
            - name: <string>
              value: <string>
        # Any combination of these auth mechanisms may be set; additional auth types
        # will be added as siblings. Validation checks the required fields of
        # whichever methods are set.
        authentication:
            # APITokenAuth configures static API-token authentication: each request
            # carries a '<key>: <token>' header, where key is the header name (e.g.
            # "Authorization", "X-API-Key") and token is the full header value (e.g.
            # "Bearer <secret>"), treated as a secret in its entirety. Top-level so
            # future non-HTTP test types can reuse it.
            api_token_auth:
                key: <string>
                token: <string>
            # BasicAuth carries HTTP basic auth credentials used by a SyntheticTest.
            # Top-level so future non-HTTP test types can reuse it.
            basic_auth:
                username: <string>
                password: <string>
            # ClientCertificate is the certificate presented during the TLS handshake
            # (mTLS).
            client_certificate:
                # PEM leaf certificate plus any intermediates.
                certificate: <string>
                # Matching unencrypted PKCS#8/PKCS#1/SEC1 PEM key. Write-only: reads return
                # **REDACTED**, and writing that sentinel back preserves the stored key.
                private_key: <string>
            # OAuth2ClientCredentials configures the OAuth 2.0 client-credentials grant.
            oauth2_client_credentials:
                client_id: <string>
                client_secret: <string>
                # OAuth2Common holds the OAuth 2.0 token-endpoint parameters shared by the
                # grants below. Top-level so future non-HTTP test types can reuse it.
                common:
                    access_token_url: <string>
                    audience: <string>
                    resource: <string>
                    scopes:
                        - <string>
                    # OAuth2TokenAuthMethod selects how client credentials are sent to the OAuth
                    # 2.0 token endpoint. OTAM_INVALID (unset) defaults to an HTTP Basic auth
                    # header (RFC 6749 section 2.3.1).
                    token_auth_method: <OTAM_BASIC_AUTH_HEADER|OTAM_REQUEST_BODY>
            # OAuth2ResourceOwnerPassword configures the OAuth 2.0 resource-owner
            # password-credentials grant. client_id/client_secret are optional, set when
            # the token endpoint also authenticates the client.
            oauth2_resource_owner_password:
                username: <string>
                password: <string>
                client_id: <string>
                client_secret: <string>
                # OAuth2Common holds the OAuth 2.0 token-endpoint parameters shared by the
                # grants below. Top-level so future non-HTTP test types can reuse it.
                common:
                    access_token_url: <string>
                    audience: <string>
                    resource: <string>
                    scopes:
                        - <string>
                    # OAuth2TokenAuthMethod selects how client credentials are sent to the OAuth
                    # 2.0 token endpoint. OTAM_INVALID (unset) defaults to an HTTP Basic auth
                    # header (RFC 6749 section 2.3.1).
                    token_auth_method: <OTAM_BASIC_AUTH_HEADER|OTAM_REQUEST_BODY>
        # Content type sent with the request. Mapped to a Content-Type request
        # header. Validation rejects setting this alongside an explicit
        # Content-Type header.
        content_type: <CONTENT_TYPE_APPLICATION_JSON|CONTENT_TYPE_APPLICATION_X_WWW_FORM_URLENCODED|CONTENT_TYPE_TEXT_PLAIN|CONTENT_TYPE_TEXT_XML|CONTENT_TYPE_TEXT_HTML|CONTENT_TYPE_MULTIPART_FORM_DATA|CONTENT_TYPE_APPLICATION_OCTET_STREAM|CONTENT_TYPE_GRAPHQL>
        # HTTP protocol version used for the request. When unset, the default is to
        # negotiate HTTP/2 with fallback to HTTP/1.1.
        http_version: <HTTP_VERSION_HTTP_1_1|HTTP_VERSION_HTTP_2>
        method: <GET|POST>
    # Alerting configuration for the test. An alert fires when at least
    # min_failing_locations locations are simultaneously failing within the
    # failing-duration window, and resolves automatically (no separate resolve
    # duration).
    monitor_config:
        # Peak count of simultaneously-failing locations within the window
        # required to fire. Must be between 1 and the number of test locations.
        min_failing_locations: <integer>
        # Failure-duration window in seconds. 0 means alert on first failure.
        # Otherwise must be at least interval_secs.
        failing_duration_secs: <integer>
        # Annotations attached to the generated alert.
        annotations:
            key_1: <string>
        # Notification policy that routes the generated alert.
        notification_policy_slug: <string>
    # Per-location retry behavior after a failed attempt. No retries when
    # unset.
    retry_config:
        # Number of retries after the initial failed attempt (0-3). 0 or unset
        # means no retry.
        max_retries: <integer>
        # Delay between attempts in milliseconds (up to 5000). 0 is treated as
        # unset and defaults to 300 when max_retries is set.
        retry_interval_ms: <integer>
    status: <ENABLED|PAUSED>
    tcp_test:
        host: <string>
        port: <integer>
        assertions:
            - # Asserts on the outcome of the TCP connection attempt. Only the EQUALS
              # operator is supported (e.g. connection is Established).
              connection_assertion:
                operator: <EQUALS>
                target: <ESTABLISHED|REFUSED|TIMEOUT>
              # Asserts on the number of network hops to the target.
              network_hops_assertion:
                target: <integer>
                operator: <EQUALS|LESS_THAN|LESS_THAN_OR_EQUAL|GREATER_THAN|GREATER_THAN_OR_EQUAL>
              # Asserts that the request response time is below the target, in
              # milliseconds. Scope selects whether DNS resolution time is included.
              response_time_assertion:
                target_ms: <integer>
                operator: <LESS_THAN>
                scope: <INCLUDING_DNS|WITHOUT_DNS>
    test_type: <HTTP|DNS|TCP|TLS>
    tls_test:
        host: <string>
        port: <integer>
        # SNI server name; defaults to host when unset.
        server_name: <string>
        accept_self_signed: <true|false>
        fail_on_incomplete_chain: <true|false>
        assertions:
            - # Asserts on a named property of the leaf certificate, e.g. subject, issuer,
              # or a Subject Alternative Name.
              cert_property_assertion:
                property: <string>
                target: <string>
                operator: <CONTAINS|NOT_CONTAINS|EQUALS|NOT_EQUALS|REGEX_EQUALS|REGEX_NOT_EQUALS>
              # Asserts on the leaf certificate's expiration window, in days.
              certificate_assertion:
                target_days: <integer>
                operator: <EXPIRES_IN_MORE_THAN_DAYS|EXPIRES_IN_LESS_THAN_DAYS>
              # Asserts that the request response time is below the target, in
              # milliseconds. Scope selects whether DNS resolution time is included.
              response_time_assertion:
                target_ms: <integer>
                operator: <LESS_THAN>
                scope: <INCLUDING_DNS|WITHOUT_DNS>
              # Asserts on the negotiated TLS version. bound selects whether the assertion
              # constrains the maximum or minimum acceptable version.
              tls_version_assertion:
                bound: <MAX|MIN>
                operator: <EQUALS|LESS_THAN|LESS_THAN_OR_EQUAL|GREATER_THAN|GREATER_THAN_OR_EQUAL>
                target: <TLS_1_0|TLS_1_1|TLS_1_2|TLS_1_3>
        # Credentials presented during the handshake.
        authentication:
            # ClientCertificate is the certificate presented during the TLS handshake
            # (mTLS).
            client_certificate:
                # PEM leaf certificate plus any intermediates.
                certificate: <string>
                # Matching unencrypted PKCS#8/PKCS#1/SEC1 PEM key. Write-only: reads return
                # **REDACTED**, and writing that sentinel back preserves the stored key.
                private_key: <string>
`

func newSyntheticTestScaffoldCmd() *cobra.Command {
	cmd := &cobra.Command{
		Use:     "scaffold",
		GroupID: groups.Commands.ID,
		Short:   "Scaffolds a complete object with placeholder values",
		Run: func(cmd *cobra.Command, args []string) {
			fmt.Fprint(cmd.OutOrStdout(), SyntheticTestScaffoldYAML)
		},
	}
	return cmd
}

func NewSyntheticTestCmd() *cobra.Command {
	root := &cobra.Command{
		Use:     "synthetic-tests",
		GroupID: groups.Config.ID,
		Short:   "All commands for SyntheticTests",
	}

	root.AddGroup(groups.Commands)
	root.AddCommand(
		newSyntheticTestCreateCmd(),
		newSyntheticTestReadCmd(),
		newSyntheticTestUpdateCmd(),
		newSyntheticTestDeleteCmd(),
		newSyntheticTestListCmd(),
		newSyntheticTestScaffoldCmd(),
	)
	return root
}
